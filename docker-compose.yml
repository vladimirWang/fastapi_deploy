services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "6000:6000"
    volumes:
      - .:/app
    working_dir: /app
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "6000"]
    # depends_on:
    #   - postgres
    #   - redis
    # networks:
    #   - fastapi-network