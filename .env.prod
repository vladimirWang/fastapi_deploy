# 宿主机直连 compose 暴露的 5432；容器内由 docker-compose.yml 覆盖为 @postgres:5432
SQLALCHEMY_DATABASE_URL=postgresql+psycopg://postgres:postgres@postgres:5432/fastapi_deploy

DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=fastapi_deploy
